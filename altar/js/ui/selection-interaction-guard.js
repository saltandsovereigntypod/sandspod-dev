/* =========================================================
   SELECTION INTERACTION GUARD
   Keeps selection click-driven and preserves mobile scrolling.
   Companion content and Living State rendering live in companion-v2.js.
   ========================================================= */

(function initializeSelectionInteractionGuard() {
  const