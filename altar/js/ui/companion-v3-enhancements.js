/* =========================================================
   COMPANION V3 ENHANCEMENTS
   Grouped relationships, field spacing, fixed stage height,
   and subtle object-type identities.
   ========================================================= */

(function initializeCompanionV3Enhancements() {
  const panel =
    typeof altarCompanionPanel !== "undefined"
      ? altarCompanion