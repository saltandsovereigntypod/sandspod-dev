{
  "name": "salt-and-sovereignty-static-site",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js && npm run validate:dev && npm run validate:production",
    "validate": "npm run validate:dev && npm run validate:production",
    "validate:dev": "node scripts/validate-static.js --environment=development",
    "validate:production": "node scripts/validate-static.js --environment=production"
  }
}
