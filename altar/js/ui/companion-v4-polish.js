/* =========================================================
   COMPANION V4 POLISH
   Normalizes accordion styling and guarantees every Companion action set
   is moved into the same compact Actions dropdown.
   ========================================================= */

(function initializeCompanionV4Polish() {
  const STYLE_ID = "companion-v4-polish-styles";

  function install